{
  "name": "rpf6-enhanced-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "find . -path './tests' -prune -o -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs"
  }
}
